# Copy to .env, fill in, then: appwrite push functions --function-id api --with-variables
# Only this one is required. CPX / BitLabs / Anthropic keys are entered afterwards
# in the app's Settings page (they're stored in the Appwrite database).

# The "password" the Settings page asks for. Any long random string.
ADMIN_SETTINGS_TOKEN=
